import {routes, routesAdmin} from './routes.config';
import cookieConfig from './cookies.config';
import productConfig from './products.config';
const config = {
  routes,
  routesAdmin,
  cookieConfig,
  productConfig,
};

export default config;
